# this is a simple test device setup that's used to compile and test the examples
esphome:
  name: components_test
  build_path: build/components_test

esp32:
  board: esp32dev
  framework:
    type: esp-idf

logger:

external_components:
  - source:
      type: git
      url: file:///run/media/Data/syncthing/programming/SDKs/esphome/esphome_BQ21088
      ref: main
    components: [bq21088]
    refresh: always

